exports.version = '1.0.1';

var plugins = [];

exports.use = function(plugin) {
  plugins.push(plugin);
}

exports.pluginInfo = function() {
  var a = [];
  for (var i = 0, len = plugins.length; i < len; ++i) {
    a.push(plugins[i].info());
  }
  return a;
}
